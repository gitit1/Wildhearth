# HANDOFF — overnight run 2026-07-11 (Fable → Opus continuation)

**Read this top-to-bottom before touching anything.** Then load the
`fable-mode` skill (`.claude/skills/fable-mode/SKILL.md`) and run under
it. This file is the master plan + exact resume state for the
unattended overnight run the owner ordered.

## Standing orders for THIS run (owner's words, binding)
- **LOCAL COMMITS ONLY — NEVER PUSH.** Pushes resume when she's back.
- Branch: `v1-foundation` only. Never master/autorun.
- tsc-green (`npm run build`) before every commit; one task = one
  commit = one WORKLOG entry (newest at top).
- Staged work, frequent local commits; if something genuinely needs the
  owner, stop that thread cleanly and write it up here — don't guess.
- **PixelLab pacing: ≤1,500 generations per day.** Balance at plan time:
  **8,583** (Tier 3, 9,355/month). Meter with `get_balance`
  before/after every batch; charges can settle late — re-meter after
  batches, budget ~1.4× margin. Tonight's allocation ≈1,100 (see waves).
- Delegate sizeable runs to subagents (Opus complex / Sonnet narrow);
  repo-writing work is STRICTLY SEQUENTIAL (one repo agent at a time);
  scratchpad/generation agents may run in parallel.

## What the owner asked for (this run's goals, her words distilled)
1. **Everything looks like ONE pixel medium** — the ground (grass,
   tilled field, paths, water) is the last smooth/painterly holdout and
   must become pixel tiles. "הכל צריך להיראות זהה, הכל פיקסלים."
2. **Character redesign** — her complaints: nose way too long (rig side
   profile), male and female are the same character, long hair draws in
   front of the body. **Resolved by decision: characters move to
   PixelLab curated sprites** (see Decisions below) — all three
   complaints disappear with baked sprites.
3. **Apply the docs** — every rule/spec in docs/ that isn't implemented
   gets implemented ("תיישם מסמכים").
4. **Variety** — 50 fish, flowers, animals, NPCs… push collections
   toward the DECISIONS bar and beyond ("תייצר 50 דגים שונים…").
5. **Screens/UI redesigned properly**, small details polished.
6. **The stall next to the farmhouse doesn't belong there** — stalls
   live in the market/town.
7. **No repetitive buildings** — variants + jitter until no two
   neighbors look alike (spares exist: `buildings/spare/`, cottages
   6+8 unused).
8. **The barn must actually DO something** (storage / animal housing).

## Decisions locked tonight (with evidence)
- **D-1 Characters = PixelLab curated sprite matrix** (owner reversed
  the code-rig decision after seeing state-consistency): spec =
  2 genders × 3 body sizes × 5 hairstyles × 5 outfits; 3 hair shades +
  skin tones via RUNTIME recolor (NOT generated). Pipeline probe
  verdict (scratchpad `char-pipeline/`): **method A** — fresh
  `create_character` per combo, standard mode, 4-dir, ~1 gen/combo;
  walk via template `animate_character` ~4 gens/combo. Faces drift
  subtly between combos (they read as siblings — acceptable for a
  "pick a look" creator; mitigate with a FIXED face descriptor per
  gender + same eye color). **Generate hair in a KEYED unnatural color
  (vivid purple)** so hair-shade recolor is clean (natural browns bleed
  into outfits — measured). Optional later upgrade: face-locked
  heroine via states (~481 gens) — owner said "expand when we want".
  The rig stays as the zero-PNG fallback (CLAUDE.md rule 1 dual-path).
- **D-2 Ground = PixelLab `tiles_pro` SEGMENTATION tiles** (probe 2 =
  GO; scratchpad `ground-probe2/final_composite.png`): 32px
  square_topdown, `outline_mode:"segmentation"` (outline mode = grid
  lattice = rejected). Terrain-to-terrain EDGES ARE CODE (Bayer-dither
  alpha mask, 0 gens, `final_composite.js` shows how). Wang
  `topdown_tileset` is REJECTED for grass (always lime; can't chain
  from tiles_pro). Production tuning: ~75% plain tiles, sparse feature
  tiles, tighter tonal range on base grass, dimmer daisies.
- **D-3 Everything-pixels:** after ground lands, audit remaining smooth
  visuals (water shimmer, weather fx, particles, vista/parallax) and
  restyle to chunky pixel language (code, no gens).
- Owner-approved earlier tonight: fable-mode skill (committed
  `79506c7`), daily gen cap, local-only commits.

## Recon results (3 of 4 legs landed; 4th in flight)
- **Docs gap matrix (landed, full detail in its section below):** game
  is genuinely whole (engines/screens/AI/MVP all built). Gaps: quest
  system+log (the one LARGE v1 gap; only guidance engine + AI stub
  exist), skill neglect-decay unbuilt, variety counts below bar (fish
  12, forage 11, recipes 7, flowers 0, trees 3 species, crops 18),
  doc drift (docs still say trees/crops are code-only), 4 open owner
  decisions, wardrobe v1-vs-v2 ambiguity.
- **Char pipeline probe (landed):** see D-1.
- **Ground probes 1+2 (landed):** see D-2. Reusable already-generated
  sets in `ground-probe2/`: sage grass 16 tiles + tilled soil 16 tiles
  (wet/dry/sprout variants) — DO NOT regenerate these two.
- **Puppeteer play-through (IN FLIGHT at handoff time):** driving the
  real game through every screen; will report screen-by-screen
  verdicts, console errors, char/stall/building evidence. Its repo
  leftovers (`package.json`, `package-lock.json`, `recon-driver.mjs`,
  `recon-faces.mjs`) are ITS to clean; if the session died before its
  report, treat as lost — re-run a light QA pass later if needed, and
  don't commit its leftovers; `git checkout -- package.json
  package-lock.json` + delete the driver scripts is safe ONLY after
  confirming no agent is still running.

## THE PLAN — waves

### Wave G (generation, parallel, scratchpad-only — LAUNCHED at handoff)
- **G1 Ground production sets** (~300 cap): re-roll tighter base grass
  + dim daisies; water+shore set; stone-plaza/cobble set; optional path
  set (or reuse soil's plain-dirt); verify with weighted-field mocks.
  Output: `scratchpad/ground-prod/`.
- **G2 Character matrix phase 1** (~450 cap): 2 genders × MEDIUM size ×
  5 hair × 5 outfits = 50 combos, method A, 48px, 4-dir rotations +
  template walk each; KEYED purple hair; fixed per-gender face
  descriptor; drift-review before walk spend. Output:
  `scratchpad/char-matrix/` (+ character IDs ledger file).
- **G3 UI kit** (~350 cap): 6 `create_ui_asset` panels (window frame,
  button set, tabs, dialogue panel, tooltip, dock) in warm wood+cream
  "cozy UO" style; 1 `create_font` (warm pixel font + TTF); 10 NPC bust
  portraits via `create_portrait_character` from the existing
  `characters/<npc>.sheet.png` south rotation crops. Output:
  `scratchpad/ui-kit/`.

### Repo queue R1..R9 (STRICTLY SEQUENTIAL, start after the relevant
gen wave lands; each = own agent, own commit(s), own WORKLOG entry)
- **R1 Character sprite creator** (after G2): copy matrix into
  `src/assets/pixellab/characters/matrix/` (pack with
  `scripts/packsheets.mjs` if loose frames bloat — `assetsInlineLimit`
  is already 0); new selection logic keyed by
  (gender,size,hair,outfit); runtime recolor of keyed-purple hair → 3
  shades + skin tones (reuse `recolorSheet`); rewire
  `ui/charcreation.ts` to the matrix options (5 hair, 3 shades, 5
  outfits, gender; sizes S/L greyed "coming soon" until G-wave 2);
  player + preview render sprite-primary, rig = fallback
  (`CHARACTER_SPRITES_PRIMARY` flips meaning or a new flag — keep ONE
  clear flag); NPCs return to their session-2 sprite sheets
  (`setNpcSpriteMode` back on). Fixes owner complaints #nose/#same/#hair-layer.
- **R2 Ground tiles** (after G1): tile renderer for the pre-rendered
  ground canvas (`world/ground.ts`) using weighted variant scatter
  (seeded), code dither edges between terrains (grass↔path↔tilled↔
  water↔plaza), keep the old painterly painter as the zero-PNG
  fallback. Farm plot + paths + market plaza + shorelines all tiled.
- **R3 Variety data push** (no gens): fish 12→**50** (owner's number;
  season/weather/location/skill framework exists), forage 11→20+,
  recipes 7→20+, crops 18→20; **flowers 0→20**: new ornamental flower
  species table wired into `systems/gardening.ts` (plant/water/bloom/
  sell), reusing the foliage flower sprites for in-world display +
  code icons. Balance prices per `data/` conventions.
- **R4 Stall + building anti-repetition:** move the farm-side stall to
  the market (zones.ts; keep the player's OWN sell-stall mechanic on
  the farm if that's what it is — check `sellCategories`/`shop`
  wiring first; if it's her own stall, keep function, restyle/relocate
  per owner note "אמור להיות דוכן בעיירה"); wire cottage variants 6+8
  + `buildings/spare/*` so no two neighbor buildings repeat; add
  per-building hue/flip jitter where tasteful.
- **R5 Barn use:** barn interaction = storage chest UI (existing
  window system; new `systems/storage.ts`, versioned save key) +
  animals shelter there at night (schedule tweak). Small, real.
- **R6 Quest system + log** (the big one): authored quest data
  (tutorial arc + 3-5 farm/NPC quests), `systems/quests.ts`
  (accept/track/complete, save-versioned), quest-log window, promote
  AI quest stub D3 → real offers behind the AI toggle with scripted
  fallback.
- **R7 Skill neglect-decay** per DECISIONS (slow decay of unused
  skills, floor at tier boundaries, `systems/skills.ts`).
- **R8 Screens re-skin** (after G3): apply UI kit nine-slice panels +
  pixel font + NPC portraits in dialogue window; keep DOM structure,
  swap the chrome. Audit all screens for the "small details" pass.
- **R9 Docs sync:** DECISIONS/HANDOFF/CLAUDE/VISION updated for D-1..3
  (char medium re-reversal!), GAME_OVERVIEW/ROADMAP_TO_V5 status
  flips (trees/crops sprites shipped, this run's additions), WORKLOG
  already per-commit. Log open owner questions (below).
- **Wave G2 (later day, if budget day allows):** body sizes S+L for the
  char matrix (+100 combos ≈ +500 gens incl. walks), NPC roster
  expansion toward the owner's bigger-roster ask (method A + AI
  dialogue seeds), fish/item pixel icons if wanted.

### Order sensitivity
R1 and R2 are independent of R3-R5; R6-R8 can interleave; NOTHING in
the repo runs in parallel with anything else in the repo. Commit after
each R-item (or sub-item if large). If a gen wave hasn't landed, skip
to the next repo item that's unblocked (R3-R7 need no gens at all).

## Open questions FOR THE OWNER (do not decide these)
1. Wardrobe buy+swap in v1 or v2? (DECISIONS reads v1, ROADMAP_TO_V5
   says v2.)
2. Face-locked heroine upgrade (~481 gens) — want it?
3. The 4 open DECISIONS items: festival theme, life-goal list confirm,
   mine location, weather-event specifics (PROPOSALS #16-19 ready).
4. Fish at 50 = data+code icons now; generate 50 pixel fish ICONS too
   (~50-100 gens)? Recommended but her call on style.

## Exact resume state (updated by the R1 repo agent)
- **R1 is DONE** (local commit `0c7aea2`, never pushed, WORKLOG entry
  "character — curated sprite matrix becomes the shipped player look"): the
  50-combo matrix is packed into `src/assets/pixellab/characters/matrix/`
  (`scripts/packsheets.mjs --matrix`), `CHARACTER_SPRITES_PRIMARY=true` now
  means the matrix player + NPC-sprite mode, `spriteChar.ts` rewritten as the
  matrix bridge (keyed-purple hair → 3 naturals, rig = zero-PNG fallback),
  `charcreation.ts` shows the sprite creator (5 hair / 5 outfit / 3 shade;
  skin-tone + body-size "coming soon"), `meta.ts` schema+revive extended (old
  saves → default matrix look). No combos excluded (ponytail-tunic re-check
  passed). Verified via headless Edge (screenshots in
  `scratchpad/char-matrix/ingame-*.png` + `crop-*.png`). Skin recolour shipped
  OFF (mechanism preserves lightness, can't darken skin — honest call).
  Follow-ups logged in the WORKLOG (unused legacy heroine sheets; S/L sizes +
  lightness-aware skin as the next matrix expansion).
- **R3, R4, R5, R7 are DONE — local commits only (never pushed), each its own
  commit + WORKLOG entry:**
  - `619144b` R3 Fish 12→50
  - `61d3fee` R3 Forage 11→26
  - `4facc09` R3 Recipes 6→21
  - `b731ddb` R3 Crops 18→20 (dual-path proven)
  - `c479f92` R3 Flowers 0→20 (new `data/flowers.ts` + species gardening system)
  - `49593b4` R4 Stall → market edge + cottage anti-repetition (variants 6/8, flip)
  - `372cad3` R5 Barn storage chest + animals shelter at barn at night
  - `2702f98` R7 Skill neglect-decay, floored at tier boundaries
  - `6adeacc` R2 Ground → pixel tiles (grass/soil/water/plaza), dual-path + code
    Bayer-dither edges; painterly painter kept as the zero-PNG fallback; the
    64 G1 tiles copied into `src/assets/pixellab/ground/`. Verified tiled +
    fallback via headless Edge (`scratchpad/ground-prod/ingame-*.png`,
    `verify_fallback.png`). See WORKLOG "world — the ground becomes pixel tiles".
- Every item verified per fable-mode (esbuild/node assertions for data + logic;
  headless-Edge screenshots for the crops fallback, flower beds+icons, the
  market/stall, and the barn storage window). `npm run build` green at each.
- **Working tree still holds the puppeteer agent's temp files** (`package.json`,
  `package-lock.json`, `recon-driver.mjs`, `recon-faces.mjs`) — NEVER committed
  by this agent (selective `git add` of own files only). Still not ours to
  commit or revert.
- **R6 is DONE — the quest system + log — 5 local commits (never pushed), each
  its own WORKLOG entry:**
  - `4343dfb` R6/1 core: `data/quests.ts` (6 authored quests + 3 AI templates,
    step kinds collect/deliver/talk/catch/harvest/reach via activity-counter +
    possession-gather flavours) + `systems/quests.ts` engine (accept/track/
    progress/turn-in/abandon/auto-complete, effect-return pattern) + `QUESTS_KEY`
    (in saves.ts GAME_KEYS).
  - `cf3dfdd` R6/2 wired into `main.ts` — event seams beside the existing
    `fireGuidance` calls (catch/forage/harvest/busk/cook/sell/talk/reach),
    reward application (coins/items/Friendship+heart), possession live-check,
    New Game wipe, save, dev bridge.
  - `fac1fd6` R6/3 quest-log WINDOW (`ui/questlog.ts`, icon 📋 / key J) —
    Active/Completed tabs, step checklist + counts, reward preview, abandon;
    a "Getting Started" box MIRRORS the live Guidance layer (coexist, not
    duplicated); subtle step-complete toast.
  - `f34bd29` R6/4 dialogue integration — offers/turn-ins as the giver's opening
    choices (`questOptions` hook in `dialoguebox.ts`, `systems/dialogue.ts` stays
    pure); accept/turn-in updates Friendship per rewards.
  - `8d44aaf` R6/5 D3 AI dynamic offers (`ai/features/questOffers.ts`, replaces
    `questStub.ts`) — validated against the closed schema (questId ∈ AI
    templates), reward clamped, scripted fallback on any failure, byte-identical
    with AI off. Verified: node harnesses (engine 33 asserts, offers 13 asserts)
    + live headless-Edge/puppeteer (accept→gather→ready→turn-in→rewards→reload→
    New-Game-wipe; dialogue offer/accept/decline; AI-off provider `none` + 0
    anthropic.com calls; AI-on offer surfacing). Screenshots in
    `scratchpad/quests/`.
- **R8 is DONE — screens re-skin + the whole polish backlog — 7 local commits
  (never pushed), each its own WORKLOG entry:**
  - `7385a64` R8 Mission A — screens re-skin with the G3 UI kit: new
    `src/ui/skin.ts` (dual-path panel/font loader), wood-frame `border-image`
    on every utility/panel window + the menu/pause/char-creation modals (dark
    readable interiors, no fill), parchment tooltip on `#prompt`,
    `WildhearthStorybook` font on titles/buttons via `--font-title`, and
    Maren/Tobin portrait busts in the dialogue notch (data-driven; other 8 NPCs
    gracefully portrait-less). Kit assets under `src/assets/pixellab/ui/` +
    `src/assets/fonts/`. Verified incl. a zero-UI-PNG fallback boot.
  - `f23c9b8` R8/B1 — Skills window shows `skillTier` (Novice/Skilled/Expert).
  - `8f3418d` R8/B3 — `STORAGE_KEY` folded into `saves.ts` GAME_KEYS.
  - `d3fb35f` R8/B2 — deleted the 5 unused legacy `characters/heroine*` sheets
    (~968KB; matrix replaced them, rig fallback intact).
  - `6ee70b5` R8/B4+B5 — plain-furrow-dominant field (`SOIL_TILLED_BAG`),
    flower/grass scatter off the plaza cobble (ground.ts + scatter.ts), and
    chunky pixel water glints (`props.ts drawWaterShimmer/drawOpenWaterShimmer`).
  - `ba7f904` R8/B6 — active-quest count badge on the quest dock icon.
  - NOT shipped from the kit (honest calls, in the Mission-A WORKLOG Follow-ups):
    the `button`/`tabs` panels (gold CSS + storybook font already read as one
    family; the wood plaque looked gaudier) and the `dialogue-panel` PNG (the
    generic window frame + a CSS notch is simpler than nine-slicing a baked
    notch). 8 more NPC portraits + S/L body sizes remain for the next budget day.
- **Repo queue remaining:**
  R9 (docs sync incl. stale `char-medium-code-rig.md` AND the char-medium
  re-reversal to matrix sprites; GAME_OVERVIEW/ROADMAP_TO_V5 quest-system status
  flip — v1's one LARGE gap is now CLOSED). **R1–R8 are DONE.**
- PixelLab: untouched by this agent (0 gens). Balance unchanged from the G-wave
  agents' accounting.

## Notes for the next repo agent
- **R8 tie-in:** R7 exposed `skillTier(value)` (Novice/Skilled/Expert) but it
  isn't shown in the Skills window yet — easy win for the screens pass.
- **R6/quests shipped:** the quest log is a real window-system citizen; the
  `talk` and `reach` step kinds are supported + unit-tested but no SHIPPED quest
  uses them yet (easy content adds). The AI offer's clamped reward is validated
  + stored but the granted payout stays the authored template's (balance-safe;
  a future refinement could let the accepted instance carry the clamped reward).
  `STORAGE_KEY` (R5) is still missing from `saves.ts` GAME_KEYS — harmless
  (New Game re-seeds via `resetStorage`) but worth folding in during R9.
- **Data tables are fully table-driven** — fish/forage/recipes/crops/flowers all
  propagate to prices/names/icons/collections/shop with zero per-item code, so
  future variety is just more rows.
- No open owner-decisions were hit; nothing was skipped for lack of a decision.

## Next step (the very first thing the continuation does)
R1–R8 are DONE (all local commits, never pushed). The ONLY remaining repo
item is **R9** (docs sync): update DECISIONS/CLAUDE/VISION for the
char-medium re-reversal (the stale `char-medium-code-rig.md`), and flip the
quest-system status in GAME_OVERVIEW/ROADMAP_TO_V5 now that v1's one LARGE
gap is closed. `STORAGE_KEY` was already folded into saves.ts GAME_KEYS
(R8/B3). Also worth a doc note: the UI kit is now wired (src/ui/skin.ts,
dual-path) and 8 more NPC portraits + S/L body sizes are the next
budget-day generation items (recipe in the kit LEDGER). Verify per
fable-mode, commit locally per item, update this file's resume state.

---

## RUN COMPLETE (2026-07-11, R9 docs-sync agent)

**R9 is DONE — every repo queue item (R1 through R9) has now shipped, each
its own local commit (never pushed), each with a WORKLOG entry.** This run
is closed out. (No `char-medium-code-rig.md` file was ever found in the
repo — the "stale doc" note above turned out to refer to
`docs/DECISIONS.md`'s "Art medium division" section itself, which is what
got annotated + superseded, not a separate file.)

### What R9 touched (docs only, one commit)
- **`docs/DECISIONS.md`** — annotated the session-3 "Art medium division"
  entry as superseded (kept in full, not deleted); added three new
  sections: "Art medium division — FINAL FLIP" (D-1, characters = curated
  sprite matrix), "Ground medium" (D-2, `tiles_pro` segmentation, Wang
  rejected), and "Everything-one-pixel-medium rule" (D-3).
- **`CLAUDE.md`** hard rule #1 — MEDIUM DIVISION clause rewritten: characters
  = sprite matrix (rig = fallback), environment = sprites (unchanged), ground
  = PixelLab tiles (painterly = fallback).
- **`docs/HANDOFF.md`** — new top "SESSION 4" section: the re-flip + probe
  one-liners, the full commit list, PixelLab spend, next-day queue.
- **`docs/GAME_OVERVIEW.md`** — Quests flipped 🟡→🟢 (full rewrite of that
  section), skill neglect-decay flipped to built, Collections variety counts
  updated (fish 50, forage 26, +flowers 20, recipes 21, crops 20 mentioned in
  context), barn storage noted under Housing, UI re-skin noted under Menus,
  the Art direction section rewritten for the FINAL medium (characters =
  matrix, ground = tiles), and the Character-creation section rewritten for
  the matrix (replacing the stale honest-coverage-table language).
- **`docs/ROADMAP_TO_V5.md`** — flagged the "Current build state (mid-v1)"
  section as a stale session-1 snapshot (pointing to GAME_OVERVIEW for
  current truth) while still refreshing its variety numbers; quests bullet
  marked BUILT; skill-decay bullet marked BUILT; the v1 "Art / visual layer"
  bullet and the "Segmented rig scope creep" risk note both rewritten for
  the session-4 FINAL flip; v4/v5 Art bullets updated from "characters = rig"
  to "characters = sprite matrix".
- **`docs/VISION.md`** — the "Medium division" paragraph under Art direction
  rewritten for the FINAL flip (light touch, one paragraph, per the brief).
- **`docs/PIXELLAB_ASSETS.md`** — §1 table's heroine/NPC rows updated (matrix
  + ground tiles + UI kit added, old heroine row removed since those files
  are deleted); a new "Wave 7" entry in §2 documenting the char-matrix/
  ground-tile/UI-kit generation recipes (keyed-purple hair convention, the
  4-dir cardinal-only matrix rotations, the Wang-tileset rejection, the UI
  panels shipped-vs-not); §3's folder tree updated with `matrix/`, `ground/`,
  `ui/`, and the fonts note; a new "Recolouring the matrix hair" section
  (the old "Recolouring the heroine" section kept, marked SUPERSEDED, for
  its underlying technique); §4's "add a hairstyle" recipe split into the
  superseded heroine path and the live matrix path; §5's cost ledger got a
  new "Wave 7" paragraph (~520 gens, Tier 3, balance ~8,063).

### Contradictions found and how they were reconciled
1. **Crops/trees were already sprite-sourced** (`src/assets/pixellab/crops/`
   and `trees/` both have real ripe/season PNGs) even though
   `GAME_OVERVIEW.md`'s Art-direction section still said "still code-only /
   not built... an approved MIX path... gated behind its own batch, not yet
   run." This predates session 4 (nobody in this run generated crop/tree
   sprites) — some earlier session shipped them without syncing this
   paragraph. Fixed while rewriting that section anyway (in-scope: the
   mission named "ground+characters art rows reflect the final mediums" and
   this row sits right next to them).
2. **`ROADMAP_TO_V5.md`'s "Current build state (mid-v1)" section** claims
   the NPC engine, Needs, Relationships, Dialogue+AI, Character Creation,
   Guidance Mode, the Festival, and the visual foundation are all "Not yet
   built" — all of these have been built and shipped for many sessions.
   This is a session-1 snapshot that was never revisited as the game grew;
   fully rewriting it was judged out of scope for a targeted docs-sync run
   (would be a large, unrelated rewrite), so it's flagged in place with a
   pointer to `GAME_OVERVIEW.md` (which IS kept current) rather than left
   silently wrong or silently deleted.
3. **The "stale `char-medium-code-rig.md`"** named in the R9 instructions
   does not exist as a file — grepped the whole repo, zero matches. Read as
   referring to `docs/DECISIONS.md`'s "Art medium division" section, which
   is exactly what got annotated + superseded.

### Verification
`npm run build` green after every edit batch (docs-only changes, so this
mainly confirms nothing else in the working tree broke) — final check run
after all seven doc files were edited. No source files touched, per the
R9 standing order ("Docs only — NO source changes").

### Working tree note (unchanged from before R9)
`package.json`/`package-lock.json` (modified) and `recon-driver.mjs`/
`recon-faces.mjs` (untracked) — the puppeteer QA agent's leftovers — are
STILL sitting in the working tree, STILL not staged or touched by this
agent (selective `git add` of only the docs this entry lists). Its recon
never reported back before this run ended; see HANDOFF.md's SESSION 4
next-day queue item 5.

**The overnight run (SESSION 4) is now fully closed out: R1 through R9,
all shipped, all local-only commits, nothing pushed.**

---

## DAY 2 (2026-07-11) — v2 progression begins, one block at a time

Standing orders unchanged: **LOCAL COMMITS ONLY, NEVER PUSH**; `npm run build`
green per commit; WORKLOG entry per commit; selective `git add` (the puppeteer
agent's `package.json`/`package-lock.json`/`recon-driver.mjs`/`recon-faces.mjs`
are STILL in the working tree, STILL never ours to stage). 0 PixelLab gens.

### v2 BLOCK #1 — DONE: "Customers come to your stall"  (commit `bc6767d`)
- **Which block the roadmap defined as #1, and why:** the supervisor guessed
  "the real sell menu," but that is **already built** (v1: `ui/shopwindow.ts`
  is a full stepper/total/sell-all/category-aware window with an NPC-stall
  mode). Per `ROADMAP_TO_V5.md` §v2's economy delta + the global dependency
  spine (`Schedules → Customers → Reputation → Town`), the genuinely-first
  UNBUILT v2 block is **`systems/customers.ts` — NPCs walk to your stall with
  want-table-driven demand** (customers.ts + reputation.ts were both absent).
  It is ALSO the "deepened own-stall selling" the brief described.
- **What shipped:** townsfolk in the plaza walk to her own stall during market
  hours, wanting a good she holds (role-based want-tables), paying a 1.3×
  premium; a "Customers at your stall" section in the trade window; serving =
  coins + the existing sell seam (guidance/quests/day-log/arcs, extracted to a
  shared `logSale()`) + Friendship +1 + memory; 🛒 bubbles; a daily served cap;
  patience timeout (no penalty — that's reputation's job). New
  `systems/customers.ts`, `Npc.visit` override, `economy.sellGoodAt`,
  `CUSTOMERS_KEY` in GAME_KEYS, `CUSTOMER_*` knobs. Old saves safe. Full detail
  in the WORKLOG entry of the same title.
- **Bonus bug fixed (in-scope, surfaced by this block):** the scale-window
  measures content height once at init while the shop's sell/buy lists are
  EMPTY, so on a fresh profile the trade window opened collapsed (~27px body,
  clipping everything — the base sell/buy too). Fixed inside the shop window:
  bounded scroll regions + `fitHeight()` on open. Confirmed the window now
  opens correctly filled (screenshots in scratchpad `v2-block1/`).
- **Verdicts (honest):** logic + serve + persistence + organic AND forced spawn
  all verified live (headless Edge + `__wh`); build green. The premium is a
  flat 1.3× (reputation will modulate it in block #2). Customer visits are
  runtime-only (a mid-market reload clears the queue — harmless).

### DOCS SCREEN — DONE (owner-requested interjection, commit `c1b8b66`)
Out-of-band owner request (not a v2 progression block): a **developer/archive
reading room** so she can audit her own planning. New **"Project Docs"** button
on the main menu opens a near-fullscreen screen that auto-globs EVERY markdown
doc (`docs/*.md`, `runs/*.md`, `CLAUDE.md`) as **lazy `?raw` chunks** — future
docs appear with zero code change and the entry bundle stays lean (all doc text
is separate hashed chunks, not the entry). Grouped sidebar (Design docs /
Roadmaps / Logs / Runs / Rules, human titles from the first `#` heading),
parchment reading pane, per-doc clickable TOC, and a **cross-doc snippet search**
that opens a doc scrolled to + highlights the match. New self-contained
Markdown→DOM renderer (`src/ui/mdrender.ts`, no deps, no `innerHTML` of doc text)
covers headings/emphasis/inline-code/links/nested+loose lists/task-checkboxes/
tables/code-fences/blockquotes/HRs. Files: new `ui/docsscreen.ts` + `ui/mdrender.ts`;
changed `ui/mainmenu.ts` + `index.html` CSS. Pure dev/meta — no gameplay/save/
boot impact. **Verified** headless-Edge (screenshots in scratchpad
`docs-screen/`): menu button, DECISIONS reader, WORKLOG (6067 lines) ~294 ms /
7034 nodes, "festival" search → 110 matches/11 docs, click-through highlight,
ROADMAP_EXPANSION 66 checkboxes + a table. `npm run build` green; local commit
only, never pushed; `package.json`/`package-lock.json`/`recon-*.mjs` still left
untouched. Follow-ups in the WORKLOG entry (reference-style links + nested tables
unsupported — no doc uses them; virtualize the reader only if a doc ever dwarfs
WORKLOG).

### v2 BLOCK #2 — DONE: town-wide Reputation / Fame  (commit `eabeb66`)
- **Doc-verified scope:** `ROADMAP_TO_V5.md` §v2 ("`systems/reputation.ts` —
  town-wide Fame, independent of any one NPC and of Haggling") + VISION §Economy
  (UO fame/karma: one town-facing number, distinct from Haggling and any one
  friendship). Built exactly to that: matches the brief, no divergence.
- **What shipped:** `systems/reputation.ts` — a single 0-100 Fame score, five
  warm tiers **Unknown → Familiar Face → Well-Liked → Respected → Beloved**,
  shown in the Skills window header (`🏛️ <tier> · NN/100`). RISES on served
  customers (+1.5), quest completion (+4), festival turnout (+3/day), warm
  gifts (loved/liked, +0.5), with a tier-crossing toast + Memory entry. FALLS
  gently on a timed-out customer (−1 — block #1's deferred penalty, wired here)
  and drifts after 5 idle days FLOORED at the current tier (neglect never
  demotes). MODULATES the customer economy live: premium is now a band
  **1.15×→1.45×** (was flat 1.3×), daily cap grows **6→10**, spawn odds gain
  **+0.25**. Fallback-safe AI-dialogue hook (Fame line in the prompt; off =
  byte-identical). New `REP_*` knobs + `REPUTATION_KEY` (in `GAME_KEYS`, so New
  Game wipes it). Files: new `systems/reputation.ts`; `config.ts`, `main.ts`,
  `systems/customers.ts` (premium/cap now parameterised), `systems/saves.ts`,
  `systems/worldContext.ts`, `systems/ai/features/dialogueVariation.ts`,
  `ui/skills.ts`, `index.html`. Full detail in the WORKLOG entry of the same title.
- **Verdicts (honest):** 32/32 logic asserts + live headless-Edge all green
  (serve-climb +1.5/sale, premium band 1.15→1.405 at fame 0 vs 85, gentle
  timeout drop, a real quest `petra_bread_run` bumps exactly +4, fame survives
  reload, New Game wipes). Screenshots in `scratchpad/v2-block2/`. Build green,
  local commit only, never pushed. **NOT built (logged Follow-ups):** Fame-gated
  "which NPCs come" (kept the early game welcoming); Fame on the HUD proper (only
  the Skills window for now); `AI_ARCHITECTURE.md` doesn't yet list reputation in
  prompt context though the hook exists (a future docs-sync line).

### v2 BLOCK #3 (next, for the supervisor to dispatch) — Town region + merchants
Per the §v2 spine (`Customers → Reputation → **Town region → merchants →
transportation → fast travel**`) and the §v2 gap list ("build the town region
and its buildings; … town merchants; minimap fast travel; transportation
vendors"), Reputation was the last purely-systemic v2 block; everything left is
gated on the **town existing**. So block #3 is the larger **Town buildout**:
expand `world/zones.ts` with the coastal town region (inn, specialized merchant
buildings) + its road, and add **specialized town merchants** (`data/npcs.ts` +
a merchant/shop wiring that buys/sells by specialty — Reputation's premium band
and VISION's "better opening prices" are ready to plug into merchant pricing).
Natural companion/precursor: the **schedule engine** the spine roots everything
on (block #1 rode the existing `atMarket`/`socializing` states; a real
day-of-week schedule makes town NPCs inhabit their homes/shops). **Fast travel
+ minimap** (a `ui/minimap.ts` already exists) is the block AFTER the town, since
it needs discovered town locations to travel between. Recommendation: dispatch
Town-region+merchants as #3, schedule engine as its sub-block or #3.5, fast
travel as #4.

### v2 BLOCK #3 — DONE: the coastal town buildout (3 local commits)
Doc-verified against ROADMAP_TO_V5 §v2 + WORLD_MAP (coastal town: inn, merchants,
homes) + VISION §town/§Economy. Shipped in three sensible commits, each with a
WORKLOG entry, **local only — never pushed**:
- `4770ac0` **part 1 — the region.** Map extended SOUTH (`MH` 30→46, `WORLD_H`
  960→1472px, both < 4096) for a coastal town below the market: cobbled street,
  inn (code-drawn `drawInn`, always distinct), 5 homes (unused cottage variants
  1/5 + 3 seed-distinct code cottages — no two alike), 3 merchant fronts (distinct
  banked SPARE stall sprites), a seafront + walkable town dock, sparse town props.
  zones.ts (`"town"` region, `regionAt`, `TOWN_STREET`/`TOWN_SEA`/`TOWN_DOCK`/
  `TOWN_SQUARE`/`INN`/`TOWN_HOMES`/`TOWN_MERCHANTS`, road spur, `STRUCTURES`,
  props), ground.ts (tiled + painterly town cobble & sea + beach dither),
  minimap, `drawStall` `themeId`, `drawDock` rect param, main.ts depth-sort,
  wildlife `regionSample` town case.
- `dd61d0e` **part 2 — merchants.** General store SELLS tools/seeds (year-round
  stock) at a reputation discount; fishmonger BUYS fish + greengrocer BUYS a new
  `produce` category, both at the reputation premium band; tailor = "coming soon".
  shopwindow merchant buy-mode + npc `priceMult`; shop.ts `MERCHANT_STOCK` +
  `discountedPrice`/`tryBuy` `extraDiscount`; reputation `reputationBuyDiscount`;
  sellCategories `produce`; interact.ts town merchant/inn/home clickables +
  `openTownMerchant`; main.ts hours-gated rep-priced windows + fame bump.
- `ca48674` **part 3 — life.** Finn (any weekday) + Bram & Liora (Tue/Thu) walk to
  town in the afternoon (`schedule.ts` `"town"` state + `townVisitsToday` +
  `townSpot`; stallkeepers stay at the market). First-arrival Memory entry +
  guiding toast; the `reach:"town"` quest event + minimap already point there.

**Verified live (headless Edge, screenshots `scratchpad/v2-block3/`):** the town
reads as a coherent coastal town from every angle (street `01-07`); merchants
trade with reputation-driven prices (`m1-m5`: fish 30→40 & hoe 12→11 as Fame
0→90; a real sale moved coins 500→540 and Fame 90→91.5); 3 NPCs mill the street
Tue afternoon + arrival toast + save/reload keeps fame/coins (`L1-L2`); **zero-PNG
boot renders the whole town via painterly/code fallbacks** (`Z1`, 239 sprite
images blocked, game booted). `npm run build` green at each commit.

**Honest verdicts:** the town FEELS like a town — inn, varied homes, three
working specialist stalls, a living street, dock + sea. Merchants have no
shopkeeper NPC (always open in daytime — a stall, not a manned counter); town
visitors just mingle (no town-specific pose / seafront busk spot). The beach is
the water-dither's mud/sand ring, not dedicated sand tiles.

**OPEN OWNER QUESTION (do not guess):** the **tailor/wardrobe** — DECISIONS reads
wardrobe-swap as v1, ROADMAP_TO_V5 §v2 as v2. Shipped as a "coming soon" counter.
Which version owns buy-and-swap wardrobe?

### WANTED SPRITES — follow-up generation list (0 gens spent this block)
Everything above is banked assets + code painters (dual-path legit). To lift the
town to full sprite fidelity, a future gen day wants:
1. A dedicated **inn** sprite (currently the always-code `drawInn`).
2. Town-specific **home** sprites — only 2 unused cottage variants existed, so 3
   town homes are the code-painter fallback; 3-4 fresh coastal-cottage variants
   would let them be sprites (keep "no two alike").
3. Optional **beach/sand ground tiles** (sand-dominant) for a proper shoreline vs
   the current mud/sand water-shore dither.
4. Optional **shopkeeper NPC** sprites if merchants should be manned (2+16 gens
   each on the proven pipeline) — and a coastal **seabird** wildlife sprite.

### v2 BLOCK #4 — DONE: paid minimap fast travel  (commit `f92c8ef`)
- **Doc-verified scope:** ROADMAP_TO_V5 §v2 ("minimap fast travel") + VISION §9
  (discovered-only, "for a small coin cost") + VISION price table (3-5/trip).
  Built to match: paid, discovered-only, time still ticks (no clock teleport).
- **What shipped:** new `systems/discovery.ts` — a versioned discovery ledger
  (`DISCOVERY_KEY`, farm seeded, New Game wipes) + 5 fast-travel nodes (farm /
  market / forest / lake-dock / town), each mapped 1:1 to a Region with a
  verified-walkable arrival tile, + pure `travelFare`/`travelMinutes` (VISION
  3-5 anchor, distance-ramped). Block #3's bespoke first-town greeting is now
  one case of the general ledger (`discoverRegion` + `celebrateDiscovery`).
  Minimap draws discovered nodes as labelled parchment pins (undiscovered =
  faint hint), a canvas click opens a parchment confirm card ("Fare N coins ·
  about M min by carriage"), and confirm → `fastTravel` charges the fare and
  fades through a real `stepGameMinute` loop to the destination (mirrors
  sleep). Guard rails via `travelGuardReason` (no travel mid-fade/menu/dialogue/
  transaction, not indoors, not to where you stand, not beyond your purse).
- **Verified live (headless Edge/puppeteer, `scratchpad/v2-block4/`):** fresh
  ledger `["farm"]`; walking discovers each region in turn; farm→town paid trip
  coins 50→45 (−5) / clock +53min (~quoted 52) / landed exactly on the town
  tile on walkable cobble; town→market −4 / +21min; guards fire ("Finish what
  you're doing first." / "You're already here."); save+reload keeps all 5
  discoveries; New Game wipes to `["farm"]`. Build green; local commit only;
  package/recon leftovers still untouched. Adds no PNGs (pins/card are code+DOM)
  so zero-PNG boot is structurally unaffected.
- **Judgment calls (owner may revisit):** remote farm hops read the 5-coin cap
  (faithful to "3-5/trip", adjacent nodes cost 3-4); festivals do NOT block
  travel (a convenience destination, not a critical lockout); PROPOSAL #15
  (Fame-discounted fares) deliberately left unbuilt (a proposal, not v2 scope).
- **OPEN OWNER QUESTION still standing (from block #3):** the tailor/**wardrobe**
  buy-and-swap — DECISIONS reads v1, ROADMAP_TO_V5 §v2 reads v2. Shipped as a
  "coming soon" town counter. Which version owns it?

### v2 BLOCK #5 — DONE: transportation vendors (the town stable)  (commit pending)
- **Doc-verified scope:** ROADMAP_TO_V5 §v2 ("boats (Fisherwoman), then horses/
  carriages tied to a town stable") + §v2 arc table ("Boats, horses, carriages;
  fast travel") + VISION §9 ("horses, carriages, boats… available to buy from
  early game, gated by money"). Block #4's fare maths was the plug-in point.
- **What shipped:** a **STABLE** at the east end of the coastal town street
  (code painter `drawStable` — timber bays, a horse's head, horseshoe sign,
  paddock rail; distinct from inn/homes/merchants) selling three vehicles via a
  new buy-only window (`ui/stablewindow.ts`, reuses the shop chrome): **Rowboat
  160 / Horse 240 / Carriage 360** (VISION anchor-scale mid-game sinks). New
  `systems/transport.ts` (ownership store, catalogue, `buyTransport`, effect
  helpers), `TRANSPORT_KEY` in GAME_KEYS. **Effects are WIRED, not just tracked:**
  the **horse** mounts/dismounts (R key + stable button) for **1.7× overland
  speed** + **stamina-free** travel + a **cheaper fare** (5→3), with a code-drawn
  horse under the lifted rider (`drawMount`); the **carriage** waives the fast-
  travel fare (→0, card reads "Free — your own carriage"); the **rowboat** turns
  both docks' "Look" into "**Take the rowboat out**", logging the Riverside
  Fisherwoman epic's ENTRY POINT (no boat-fishing/diving/net built — that's the
  epic). Fare discount flows through one `effectiveFare` → the minimap gained a
  `fareOf` hook so the card shows what fastTravel charges.
- **Verified live (headless Edge, `scratchpad/v2-block5/`):** fresh all-false;
  stable opens (real interactable), buy horse via UI sinks coins 1000→760;
  fare 5→3 (horse) then →0 (carriage, "Free"); mount ride measured **1.70×**;
  both dock labels flip on rowboat buy; **save/reload keeps ownership** (mounted
  resets, by design); **New Game wipes**; **zero-PNG boot** (477 image fetches
  blocked) renders the stable + mounted horse via code painters, zero errors.
  `npm run build` green; local commit only, never pushed; package/recon leftovers
  still untouched.
- **Judgment calls (owner may revisit):** the roadmap files the BOAT under the
  Fisherwoman epic — I sell it here + wire the dock entry point, but build none
  of the epic. The carriage makes fast travel FREE once owned (faithful to "your
  own coach"; owner may prefer a token residual fare). `mounted` is a session
  flag (ownership persists; she dismounts on reload). Prices are my anchor-scale
  read (boat < cow < horse < carriage), not a doc-fixed number.

### WANTED SPRITES — v2 BLOCK #5 follow-up generation list (0 gens spent)
Everything shipped is code painters (dual-path legit — proven by the zero-PNG
boot). A future gen day wants, in priority order:
1. **Horse-with-rider sprite** (4-dir walk + idle) — the mounted visual is a
   code-drawn horse right now; a real mount sprite is the top want. Also a
   riderless **horse** sprite for the stable bays.
2. A dedicated **stable** building sprite (currently the always-code `drawStable`).
3. A **rowboat** sprite (moored at the dock, and/or a "rowing out" pose) for when
   the Fisherwoman boat epic lands.
4. Optional **carriage** sprite (a parked coach by the stable) if transport should
   be visible in the world beyond ownership.

### v2 BLOCK #6 (next, for the supervisor to dispatch) — the Riverside Fisherwoman epic
Per the §v2 spine, transportation was the last self-contained block; what remains
in §v2 is the **large signature content arc** (ROADMAP_TO_V5 line 357 "the
Fisherwoman block alone is large"; interpretation note #4). Its ENTRY POINT is
already wired (block #5's dock "row out" + the owned rowboat). Dispatch as a
multi-commit arc: rod tiers + bait → teaching → the boat's real use (boat-fishing
from the dock, gating diving/net/deep-water fish) → sailing → aquarium →
binocular sightings (birds/animals/flowers into the collections engine). The
Fisherwoman's trait-derived gift preference (rare aquatic items) is the first
live instance of VISION §6's trait-to-preference mapping. **Still open before the
tailor:** the wardrobe v1-vs-v2 owner question (below) — does NOT block the
Fisherwoman.

### v2 BLOCK #6 SLICE 1 — DONE: the Riverside Fisherwoman herself + rod tiers + bait + teaching (3 local commits)
Doc-verified against ROADMAP_TO_V5 §v2 (her *basic* presence = dialogue + fishing
shop + teaching, line 47-50), VISION §Fisherwoman/§6/§skills-teaching/§gating +
price table, WORLD_MAP river, DECISIONS teaching/selling-paths. This is **slice 1
of the 4-part epic** (rod tiers/bait + teaching); explicitly OUT (later slices):
boat-fishing, diving, net, sailing, aquarium, binocular sightings — the block-5
rowboat dock entry stays a teaser. Shipped in 3 sensible commits, **local only —
never pushed:**
- `f3b2ce9` **slice 1/1 — the NPC.** Nerys, the 11th roster member: a weathered
  river sage (new `role:"fisherwoman"` + `personality:"weathered-sage"`) at the
  east river bend, dawn-to-dusk on the shore between the two river fishing spots,
  authored dialogue + backstory + trait prefs (VISION §6: rare fish loved, any
  fish liked, river-found junk liked — her signature). Renders via the **code rig
  fallback** (no sprite sheet — dual-path). `data/npcs.ts` (+`RIVERSIDE_REST`),
  filled the exhaustive Role/Personality tables (`customers.ts`,
  `traitPreferences.ts`, `thoughts.ts`, `dialogue/shared.ts`), `schedule.ts`
  (riverside socializing), `npc.ts` (face/pose the water), `dialogue/nerys.ts`,
  `backstories.ts`.
- `aea5ec4` **slice 1/2 — rod tiers + bait.** New `data/fishinggear.ts`
  (River Rod 30c/floor 15/+12q/0.85×bite; Master Rod 75c/floor 40/+25q/0.72×bite,
  trust-gated) + baits (Worm 3c, Spinner 10c rare-shifting/floor 20) +
  `systems/fishinggear.ts` (`computeCastGear`, gear pays only at/above its floor,
  cheapest bait spent first). `fishing.ts` threads `biteMult`+gear through
  `startCast`/`resolveCatch` (effective skill + rare-bias). New `ui/fisherwindow.ts`
  buy window + a generic `serviceOptions` dialogue seam. Bought from HER, not the
  general store.
- `81b4fb0` **slice 1/3 — teaching + the introduce-her quest.** New
  `systems/teaching.ts` ledger (`TEACHING_KEY`, one lesson/teacher/day) + a
  deterministic `teachSkill()` (respects the per-skill 100 + total SKILL_CAP caps).
  A paid dialogue lesson (18c) bumps Fishing beyond the grind with a scripted beat;
  3 lessons earn her trust → the Master Rod unlocks (or a proven Fishing 55). Intro
  quest `nerys_reading_the_water` (catch 3 → turn in at Nerys) through the EXISTING
  quest system.

**Verified (headless Edge + puppeteer-core over `__wh`; screenshots
`scratchpad/v2-block6a/01..05`):** roster is 11 with Nerys at the river (dialogue
in her voice, backstory, save/reload); her gear window buys River Rod+bait (coins
200→167), Master Rod "Locked" pre-trust; a **23/23 gear logic harness** proves the
odds (junk 29%→22%, rare fish 0→4 233 with gear at Fishing 20); a **16/16 teaching
harness** proves the cap-respecting bump; live lessons sink coins + climb Fishing
10→23, once/day gate holds, 3 lessons flip the Master Rod to "Buy"; the intro quest
offers→accepts→turns in (coins +15, +3 worms, carp consumed, Friendship 12);
reload keeps lessons+skill, New Game wipes both; **zero-PNG boot** (238 runtime
images blocked) renders her + the whole slice via code painters, 0 page errors.
`tsc --noEmit` green at each commit.

**Honest verdicts:** she FEELS like the signature riverside NPC — a distinct
weathered look (oilskin hat + teal coat via the rig), her own slow sage voice, a
real gear shop, real teaching, and an intro quest, all wired through the existing
seams. The gear shop's default window width slightly clips the price/Buy column
with her longer rod blurbs (resizable; buying works). Lessons are a dialogue beat,
not a mini-scene. She's a mentor, not a romance candidate (scope call). The rowboat
epic (boat-fishing/diving/net/sailing/aquarium/sightings) is untouched — that's
slice 2+.

**⚠ BUILD BLOCKER (pre-existing, owner action needed):** `npm run build`'s **vite
production step fails at HEAD** in this environment — **Node v24.14.1** + vite 6.x
trips `vite:html-inline-proxy` "No matching HTML proxy module" on `index.html`'s
inline `<style>` (reproduces on a clean HEAD checkout, clean source; both vite
6.3.5 and 6.4.3 fail; not the `assetsInlineLimit`). `tsc --noEmit` is green and the
dev server + game run fine, so it did NOT block this slice — but it blocks the
"build green per commit" bar for everyone until fixed (likely a Node downgrade to
20/22, or moving index.html's inline CSS to an external stylesheet vite imports
normally). I did NOT touch vite.config/package.json for it (out of my named scope);
I reverted the dead recon agent's uncommitted `puppeteer-core` package.json/lock
change back to HEAD (safe — no agent running) so those files are clean again.

### WANTED SPRITES — v2 BLOCK #6 slice 1 follow-up generation list (0 gens spent)
Everything shipped is code painters + banked assets (dual-path legit — proven by
the zero-PNG boot). A future gen day wants, in priority order:
1. **Nerys walk sheet** — a `characters/nerys.sheet.png` (+ .json) on the proven
   pipeline (2 + 16 gens) so she's sprite-backed like the other 10, not the rig.
2. **Nerys dialogue portrait** — `ui/portraits/nerys.png` (data-driven; drops in
   with zero code change) for her dialogue-box bust.
3. Optional rod/bait item icons (River/Master Rod, worms, spinner) if the bag/shop
   should show pixel icons rather than the current emoji.

### v2 BLOCK #6 SLICE 2 (next) — the boat's real use
The rowboat dock entry (block #5) + this slice's gear/teaching are the setup; the
next slice is **boat-fishing from the dock** — turn the owned rowboat's "row out"
into a real deep-water fishing mode that gates diving / net fishing / deep-water
fish species (ROADMAP_TO_V5 §v2 "the Fisherwoman's boat gates diving/net/deep-water
fish", line 354). Then sailing → aquarium (home tank for duplicate rares) →
binocular sightings (birds/animals/flowers into the built collections engine).
Nerys' shop + teaching are the plug-in points (she'd sell the net + teach the
deeper technique).

### v2 BLOCK #5 — original dispatch note (superseded by DONE above) — transportation vendors / the Fisherwoman
Per the §v2 spine (`Town region → merchants → transportation → fast travel`) and
the §v2 gap list (line 347-350), fast travel was the last *systemic* v2 block.
What remains in §v2 is **content-heavy** and splits two ways:
1. **Transportation vendors (recommended next — the smaller, self-contained
   block):** buyable/rentable **boats then horses/carriages tied to a town
   stable** (VISION §9; ROADMAP_TO_V5 line 332 "Transportation (new): boats
   (Fisherwoman), then horses/carriages"). Owned vehicles speed real walking and
   could reduce fast-travel fares — the discovery ledger + fare maths (this
   block) are the ready plug-in point. A stable NPC/counter reuses the town
   merchant wiring from block #3.
2. **The Riverside Fisherwoman's full kit (the large signature block, likely
   several sub-blocks):** rod tiers, bait, teaching, the boat (first
   transportation vendor + barter), diving, net, sailing, aquarium, sightings.
   The boat gates diving/net/deep-water fish. This is the §v2 "Fisherwoman block
   alone is large" epic (line 357) — dispatch as its own multi-commit arc.
Recommendation: dispatch **transportation vendors** as #5 (short, plugs into this
block), then the **Fisherwoman arc** as #6+. The wardrobe open-owner-question
above must be answered before building the tailor.

### CHARACTER — Small/Large body sizes go live (matrix phase 2) — DONE (commit `8f3df13`, local only)
Not a v2 progression block — the sprite-matrix S/L expansion the earlier handoffs
flagged as a future generation item. The 100 phase-2 combos (2 sizes × 2 genders
× 5 hair × 5 outfits; custom `proportions` for a real slighter/broader spread)
were generated off-repo (scratchpad `char-matrix/`, ledgers `ledger-small.json` /
`ledger-large.json`, 0 gens spent by this repo agent). This block packed + wired
them, mirroring R1's path:
- `scripts/packsheets.mjs` `packMatrix` parses `<gender>` (medium, unsuffixed) or
  `<gender>-<small|large>` → `matrix-<gender>[-<size>]-<hair>-<outfit>.sheet.*`.
  Repacking the full root left the 50 medium sheets byte-identical (0 modified).
- 100 new atlases under `src/assets/pixellab/characters/matrix/` (auto-globbed by
  the manifest; anchors measured per-sheet, footY 59–61).
- `src/art/spriteChar.ts` size bridge (`matrixSheetId` gains `BodySize`,
  `sizeFor(a)` junk→M); `src/ui/charcreation.ts` Body Size row live (S/M/L
  `labelGroup` + Randomize); `src/main.ts` `__wh.setPlayerSize` A/B bridge.
  `meta.ts` already had the `bodySize` schema + tolerant revive (R1).
- **Real bug fixed in verification:** the narrow SMALL builds leak DESATURATED
  violet hair (hue 255–305, sat 0.20–0.35) past the primary purple recolor band →
  added `PURPLE_BAND_SOFT` (hue floor 255 keeps denim untouched).
- **Verified** (headless Edge + puppeteer, screenshots scratchpad
  `char-matrix/ingame-sizes-*`): creator S/M/L side-by-side per gender (silhouette
  differs, no clip, hair recoloured); in-world S/M/L feet-anchored at one fixed
  spot; create-at-Large survives save→reload→Continue; zero-PNG boot (679 images
  blocked, 0 errors) renders the rig. `npm run build` green (entry 604→748 KB raw
  / 184.4 KB gzip — the 100 `.sheet.json` maps inline; the 100 PNGs are ~4 MB of
  separate hashed assets).
- **Left for a future day (niceties, not blockers):** NPC body-size variety
  (roster is single-size); lazy-loading the inlined `.sheet.json` maps; skin-tone
  still "coming soon" (H&S recolour can't darken skin — unchanged from R1).

### CHARACTER — actions keep HER sprite (tier-1, no mid-action rig swap) — DONE (commit `8e997de`, local only)
Owner complaint: during actions (fishing/hoeing) the chosen matrix sprite fell
back to the code rig — "a different person." Tier-1 fix: while acting, keep
rendering the CHOSEN sprite (facing frame + subtle action rhythm) and overlay the
tool (rod/hoe/lute/basket) as a code-drawn sprite at a hand anchor, killing the
identity swap. Sleeping still uses the tucked-in-bed fallback. Build verified
GREEN (`built in 4.33s`, tsc pass). The agent that authored this DIED on the
Claude/API monthly-spend-limit AFTER committing — work landed cleanly, tree clean.
Tier-2 (real pixel action animations, body-pose-only + code tool overlay) is the
follow-up, validated by the probe below.

### FACE-LOCK + ACTIONS PROBE — DONE (verdict, no repo change) — `scratchpad/facelock-probe/VERDICT.md`
De-risked the owner's "ONE face-locked character per gender, all states from her"
directive before any mass spend. Verdicts:
- **Q1 face-lock via `create_character_state` (~20 gens each): CONFIRMED.** Face
  stays pixel-identical across hair+outfit edits; stacks to 2 levels. Method-A
  combos drift; the state column stays one face. This is the win.
- **Q2 actions keep identity; tool drawn UNRELIABLY by PixelLab (1px, vanishes
  south).** Recommendation = animate body pose only, overlay tool in code at a
  hand anchor (one pose serves many tools) — exactly what `8e997de` does.
- **Q3 cross-size face-lock IMPOSSIBLE:** `create_character` has no `seed`,
  proportions are base-gen-only → S/M/L are 3 independent gens, faces drift per
  size. **Medium = the true canonical hero; S/L = near-matches / NPC variety.**
- **Q4 bill (both genders):** Medium library = **1,162 gens (~1,570 w/ margin) =
  1 day**; M+S+L = 3,486 (~4,700) = 3 days. Architecture: 6 bases → 24 states each
  (5×5 stacked) → walks pre-built → actions LAZY for the chosen look (16 gens) →
  tools in code → Medium hero. A ~21-gen "step 0" must first verify states inherit
  custom proportions before any S/L state batch. Two API gotchas carried forward:
  gate on the download endpoint (423→200) not the `completed` status; fetch
  rotations directly by charId (group ZIP auto-names ambiguously).

### ⛔ BLOCKER (2026-07-11): Claude/API MONTHLY SPEND LIMIT HIT
Two live agents (tier-1 action fix, item-icons batch) died with "You've hit your
monthly spend limit · raise it at claude.ai/settings/usage". This is the
**Claude/API** budget, NOT PixelLab (PixelLab still had ~7,195 gens). No new
execution subagents can be spawned until the limit resets or the owner raises it.
Item-icons run is PARTIAL (50 fish + tools + dishes to batch-2 generated in
scratchpad `icons/`) and needs resume + integration when budget returns.

### OWNER-QUESTION (blocks the character mass run — do NOT decide)
**Medium-only face-locked hero (clean single face, ~1 day / ~1,570 gens) vs. all
three sizes (S/L faces drift from the hero, ~3 days / ~4,700 gens)?** The probe
proved cross-size face-lock is an API hard-limit, so "3 sizes" no longer means
"same face at 3 sizes." Awaiting her call before committing the mass generation.

### RESUME STATE (when budget returns)
Sequential repo queue, build-green + WORKLOG per commit, local-only:
1. Integrate pixel-audit sprites (outhouse, hedge×2, dock, flower-bed, busk-sign,
   stable, inn — all PASS in scratchpad `pixel-audit/`) dual-path.
2. Integrate item icons (scratchpad `icons/`) into `art/icons.ts` dual-path;
   resume the icon-gen tail (flowers/forage/produce) if desired.
3. Character mass run per the owner's size decision + the ~21-gen step-0
   proportions check. Update WORKLOG + Hebrew hub after each.

### DAY 2 — main-loop "everything-pixels" integration sprint (spend-limit workaround) — DONE
With the Claude/API monthly spend limit blocking new execution SUBAGENTS (but
the main loop still functional), Fable integrated every already-generated pixel
asset directly in the main loop, one atomic build-green + screenshot-verified
commit each (local only, never pushed):
- `2fb9b72` outhouse + hedge (owner's two personally-caught holdouts) → dual-path
  sprites; hedge tiles down its N–S strip alternating 2 variants + flip.
- `82743aa` town inn + stable → building dual-paths; **retires the
  `fillText("INN")` canvas-text glyph** (sprite bakes a pixel sign).
- `fdf76b4` dock (fill-rect top-down deck, flipped so posts land south) + busk
  signpost (base-on-ground; note: occluded by the barn roof at the default
  camera — pre-existing placement, affects the code fallback too).
- `4bf3fda` **item icons** — 84 pixel icons (50 fish + 25 tools + 9 dishes) via a
  single dual-path seam in `drawItemIcon` (covers backpack/shop/HUD/toasts).
  Fish were the owner's explicit complaint; verified live in the Backpack window.
All screenshots in `scratchpad/pixel-audit/shots/` + `scratchpad/icons/`.

**What's LEFT (all blocked on the spend limit or an owner decision):**
1. Flower-bed frame → still code-drawn; the generated `flower-bed.png` bakes
   blooms in, so a *soil-only* regen is needed (PixelLab gen — blocked).
2. Remaining item icons → flowers (20) + forage (26) + produce (20) + junk (3);
   seeds (41) deliberately deferred. Resume gen per `scratchpad/icons/LEDGER.md`.
3. Face-locked character Medium mass run (~1,162 gens) → blocked on BOTH the
   spend limit AND the open owner size-decision (Medium-only vs all-3-sizes).
   Run the ~21-gen step-0 proportions check first.
Net: with agents blocked, the productive main-loop backlog is now EXHAUSTED —
every generated-but-unintegrated asset is shipped. Everything remaining needs
budget to reset/raise or the owner's size call.

### DAY 2 — item icons 100% pixel (owner re-opened PixelLab budget) — DONE
After the owner raised the PixelLab ceiling ("use it — but not for the character
now"), two background gen agents (spend-limit now lifted, agents work again)
filled the rest of the icon backlog; Fable integrated each in the main loop:
- `356c9cc` flowers (20) + forage (26) + produce (18) + junk (3) icons + a
  pixel flower-bed soil fixture (drawFlowerBed soil-oval → sprite, blooms still
  layer in code). Verified in-game (backpack + flower beds, 497/497 sprites).
- `31a85ae` 41 seed-packet icons (distinct kraft envelopes, each showing its
  plant). **Item-icon library is now 192 PNGs — 100% sprite coverage**; code
  painters remain only as the zero-PNG fallback.
Icon gens this phase: 68 + 50 = 118 (balance ~7162 → ~7044). Montages +
LEDGER-part2/3 in `scratchpad/icons/`.

**Session net (DAY 2 main-loop sprint, all local, never pushed):** 8 commits —
tier-1 action fix, 6 everything-pixels blocks (outhouse+hedge, inn+stable,
dock+busk, 3 icon blocks), + the flower-bed. The "everything-pixels" directive
is COMPLETE except the owner-DEFERRED face-locked character and optional
festival-prop polish (harvest cluster / lantern / bunting — festival-only, AUDIT
rated low). Nothing else in the pixel backlog is unblocked.

**Owner steer needed for the next big block:** character face-lock rebuild is
deferred by her ("not for the character right now; no body sizes"); the other
open directions are gameplay (barn utility, farm-stall→town, screen redesigns)
and variety — none is a clear autonomous pick without her priority call.

### DAY 2 — "flow with yourself" wave: gameplay + world completion (owner opened PixelLab budget) — DONE
Owner: "everything-pixels done for items+world, do it ALL, flow yourself, use the
PixelLab budget — but not the character yet." Full autonomy. Ran pixel-gen agents
in the background (scratchpad) while doing gameplay/code in the main loop and
delegating the two big self-contained features to Opus repo-agents (one git
writer at a time). Commits this wave (all local, never pushed):
- `7ec5375` animal-produce pixel icons (milk/egg/duck_egg/wool; truffle reuses the
  existing forage item + icon).
- `8980149` **BARN DOES SOMETHING** (Opus) — animal-produce collection loop, the
  VISION §122 core loop: feed owned animals → overnight they leave produce
  (cow→milk, hen→egg, duck→duck_egg, sheep→wool, pig→truffle) into the barn →
  collect there. Feed gates it; new-day hook yields; barn window shows "your
  animals left …". New `data/produce.ts` + `systems/animalProduce.ts`,
  `PRODUCE_KEY` in GAME_KEYS, prices in config. Truffle de-duped to the single
  forage item at price 18. Full-loop verified (11 assertions).
- `02b49f7` festival decorations → sprites (harvest-cluster ×2 alternating +
  lantern pole; the flicker GLOW stays code over the unlit lantern sprite).
  Verified on a forced Harvest-Festival day. Last code-drawn world object done.
- `feafd2f` **STALL → TOWN** (Opus) — the player's selling stall moved from the
  market's west edge to the coastal town (tile 63.5,33.4 on TOWN_STREET). Buyer
  pool restricted to town-present NPCs for a short clean walk; townVisitsToday
  broadened for a crowd; old market spot dressed with a cart+crate cluster.
  Full loop verified (renders no-clip, flat sell, customer walks & serves a
  premium sale, old save ok). CAVEAT (owner may want changed): town custom is
  afternoon-only (15:00–18:00); TOWN_HOMES are decorative (no NPC resides there)
  — widen hours or add residents for all-day custom.

**Honest miss:** I spun up a "building variety" gen agent to make cottage
variants 6+8, having checked `buildings/` but NOT `buildings/spare/` — where
`cottage-06_slate-stone-porch.png` + `cottage-08_shingle-plank-leanto.png`
ALREADY exist (committed Jul 7) and already back COTTAGE_SPRITES[6]/[8]. All 8
market cottages were already distinct sprites; the town has no building
repetition. My regenerated pair was redundant and was NOT integrated (scratchpad
only — no repo pollution; cost ~3 cheap gens). Building-variety directive: already
satisfied.

**Remaining owner directives:** (1) character face-lock rebuild — DEFERRED by the
owner ("not the character now, no body sizes, later"). (2) "Redesign the screens
better, invest in small details" — broad + subjective (spans main menu, char
creation, backpack, shop/trade, barn, skills, day-end, dialogue…); needs the
owner's steer on which screens + aesthetic direction before churning UI. Every
other directive from this session is shipped.

### DAY 2 — screens redesign pass ("invest in small details" + "the main screen isn't pixel") — DONE
Owner: "stop asking, just build; the main screen isn't pixel, its whole
background must change; run with what you feel." Four screens, all local commits,
build-green + screenshot-verified, none pushed:
- `57b3730` **TITLE SCREEN → pixel** (her explicit ask). `drawVista` rewritten:
  a PixelLab-generated wide pixel-art sunrise-farm scene (`ui/title-vista.png`,
  1 gen, cover-fit no-blur) is the PRIMARY background; a code-drawn pixel vista
  (low-res buffer + nearest-neighbour + Bayer dither, mirroring ground.ts) is the
  zero-PNG fallback; `drawLogo` pixel-ified. New VISTA_* config knobs.
- `0d9d0f2` **trade/shop window** small-details pass — tabular right-aligned
  numbers, uniform rows with crisp pixel icons, a "Sell everything" grand-total
  bar, a gold +premium badge on customer offers, stepper/button states, min-width
  fix so buttons never clip. Pure presentation, trade logic untouched.
- `980dcb9` **end-of-day summary** redesigned — a coin hero (net coins big in
  gold), an aligned receipt of the day's tallies, gold Skills/Bonds sections with
  +N values. New DEV-only `__wh.showDayEnd()` (import.meta.env.DEV) for layout
  verification. Pure presentation.
- `93e2631` **character-creation screen** small-details pass (UI/CSS ONLY — the
  character SPRITE pipeline was explicitly NOT touched, per the owner's deferral):
  framed preview stage (crisp `image-rendering:pixelated`) + live name/age/gender
  caption, gold selected states + focus rings, segmented Gender/Body-size
  controls, colour-dot hair shades, an intentional "Coming soon" lock chip for
  skin tone, a Randomize/Continue footer bar, and a narrow-viewport media query.

**Session status:** every directive the owner gave this session is now shipped —
everything-pixels (items 100% + all world objects incl. festival), the barn
gameplay loop, stall→town, the pixel title screen, and a four-screen polish pass.
The ONLY outstanding owner item is the DEFERRED character face-lock sprite rebuild
(Medium-only, ~1,570 gens) — awaiting her go-ahead. All work is local on
v1-foundation; nothing pushed (her call). PixelLab balance ~7,030.

### DAY 2 — "run to v5" plan handed off (Fable → Opus)
Owner: "run, as far as I'm concerned, up to v5 — Fable, prepare an organized
plan, then I'll hand it to Opus." Shipped `docs/EXECUTION_PLAN_V5.md`
(commit `798305d`, local): the operational build-order companion to
ROADMAP_TO_V5 — an 11-rule executor contract, 4 owner gates (G1 character
rebuild stays FROZEN until her yes; G2 mine access; G3 town hours; G4 push),
~40 numbered blocks (V1-C1..C3 closeout → V2-B1..B12 town → V3 crafting →
V4 family → V5 product-complete) each with acceptance criteria + SEAL
rituals, the puppeteer/__wh verification playbook, and a sizing-honesty
note. Hebrew hub republished with a log-plan entry. **Next session (Opus):
read EXECUTION_PLAN_V5.md §0–§2 first, then start at V1-C1.**

### DAY 2 — windows: logical open placement + never-clipped (owner mobile complaint)
Owner: "menus must open in a logical order, not thrown on the screen; on
mobile they get really cut off." Commit `d0032fe`. Reproduced first (390×844:
Settings −165px both sides, Tools dock −148px with Save/Settings/Pause
unreachable, quests −43px; desktop: hardcoded-defaultRect piles). Fix, in
`src/ui/windows/`: (1) OPEN placement — centered cascade +26px per open
window (`autoPlace()`), `openAt` anchors for dialogue/minimap/debug, chrome
opted out; (2) `userPlaced` flag (persisted `up`) — a player-dragged window
keeps her spot on reopen, presets clear it; (3) fully-on-screen clamp
(`clampRect`/`clampFor`) at create/open/setRect/applyLayout/resize, resizable
w/h capped to desktop; (4) CSS: `.wh-window` max 100%, non-viewport bodies
scroll, `#tools` wraps. Old layouts heal (no `up` ⇒ autoplace). Verified
both viewports: clip=0 everywhere; open→close→reopen returns to center;
dragged (666,373) reopens at (666,373); poisoned x=5000 heals; zero page
errors; build green. Follow-up (cosmetic): a stacked mobile HUD preset.

### DAY 3 (2026-07-14) — v1 sealed; v2 underway; the stale-server trap
Owner: "רוצה להמשיך?" → continued per EXECUTION_PLAN_V5. Chain (all local):
- `61b3cfd` **V1-C1 — v1 audit & seal.** No un-annotated v1 gap; real-flow
  smoke 8/8 (title→char-create→day1→sleep→day-end→Continue). The audit's one
  real find FIXED: the slot manifest only stamped on manual save/10-min
  autosave — sleep-then-quit left Continue DISABLED ("No saved game yet")
  with the whole game saved. Day rollover is now a canonical save point.
- `37fd06e` **V1-C2 — verify/ harness.** Real day-2 fixture (28 keys) +
  self-contained `npm run verify:save` (3/3) / `verify:smoke` (8/8).
- `65bcdb2` **V2-B1 (Opus agent)** — town audited: already built beyond spec;
  one real gap filled (town-square busk spot, additive). Zero gens.
- `a832eaf` **V2-B1b (same agent, correction pass)** — my eyeball caught 3
  CODE-painter town homes (everything-pixels violation); agent generated 3
  coastal cottage variants (9/10/11), 3 gens (7030→7027), dual-path kept.
- `9bdbbd3` **verify lib fix** — the B1b verification hunt exposed that on
  Windows shell-wrapped spawn + kill() ORPHANS vite; orphaned servers served
  stale code to later runs (my "smooth cottage" shots were the pre-B1b
  server!). lib now spawns vite unwrapped + refuses an already-answering
  port. Both verifies green back-to-back.
OPEN FOR OWNER: two busking spots now exist (market + town) — keep both or
retire the market one? (Liora's schedule + travel node ripple.) NEXT: V2-B2
(residents & schedules) delegated; then B3 customers/wants, B4 reputation.

### DAY 3 (2026-07-14, cont.) — the owner's window/map feedback arc, closed
Owner-driven fix chain (each verified on HER real scenario, all pushed to
origin v1-foundation AND master per her ask):
- `218e4f0` map+panels true size (measured chrome, contentSized, size heal)
- `1cc2930` UO-gump drag (whole frame) + free-space open placement
- `6ac966c` story mode (new-game flow hides all windows) + map off-by-default
  + edge-seeking panels
- `6d626cf` dev port → http://localhost:7777 (strictPort)
- `430c71a` **the UO RADAR** (corner crop of local surroundings, always-on,
  chrome-class) + the world map now opens CENTERED on demand (M)
Owner vision recorded in memory `owner-vision-core` (single-player UO,
AI-deep bonds, Sims mix, ever-more choice; UO client = UX benchmark) and
`uo-window-feel` (incl. her reference screenshot's band layout). NEXT:
her feel-check on the radar; then V2-B3 (customers & wants). Open owner
questions: two busk spots?; G3 all-day custom OK?
