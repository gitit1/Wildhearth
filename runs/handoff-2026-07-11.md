# HANDOFF — overnight run 2026-07-11 (Fable → Opus continuation)

**Read this top-to-bottom before touching anything.** Then load the
`fable-mode` skill (`.claude/skills/fable-mode/SKILL.md`) and run under
it. This file is the master plan + exact resume state for the
unattended overnight run the owner ordered.

## Standing orders for THIS run (owner's words, binding)
- **LOCAL COMMITS ONLY — NEVER PUSH.** Pushes resume when she's back.
- Branch: `v1-foundation` only. Never master/autorun.
- tsc-green (`npm run build`) before every commit; one task = one
  commit = one WORKLOG entry (newest at top).
- Staged work, frequent local commits; if something genuinely needs the
  owner, stop that thread cleanly and write it up here — don't guess.
- **PixelLab pacing: ≤1,500 generations per day.** Balance at plan time:
  **8,583** (Tier 3, 9,355/month). Meter with `get_balance`
  before/after every batch; charges can settle late — re-meter after
  batches, budget ~1.4× margin. Tonight's allocation ≈1,100 (see waves).
- Delegate sizeable runs to subagents (Opus complex / Sonnet narrow);
  repo-writing work is STRICTLY SEQUENTIAL (one repo agent at a time);
  scratchpad/generation agents may run in parallel.

## What the owner asked for (this run's goals, her words distilled)
1. **Everything looks like ONE pixel medium** — the ground (grass,
   tilled field, paths, water) is the last smooth/painterly holdout and
   must become pixel tiles. "הכל צריך להיראות זהה, הכל פיקסלים."
2. **Character redesign** — her complaints: nose way too long (rig side
   profile), male and female are the same character, long hair draws in
   front of the body. **Resolved by decision: characters move to
   PixelLab curated sprites** (see Decisions below) — all three
   complaints disappear with baked sprites.
3. **Apply the docs** — every rule/spec in docs/ that isn't implemented
   gets implemented ("תיישם מסמכים").
4. **Variety** — 50 fish, flowers, animals, NPCs… push collections
   toward the DECISIONS bar and beyond ("תייצר 50 דגים שונים…").
5. **Screens/UI redesigned properly**, small details polished.
6. **The stall next to the farmhouse doesn't belong there** — stalls
   live in the market/town.
7. **No repetitive buildings** — variants + jitter until no two
   neighbors look alike (spares exist: `buildings/spare/`, cottages
   6+8 unused).
8. **The barn must actually DO something** (storage / animal housing).

## Decisions locked tonight (with evidence)
- **D-1 Characters = PixelLab curated sprite matrix** (owner reversed
  the code-rig decision after seeing state-consistency): spec =
  2 genders × 3 body sizes × 5 hairstyles × 5 outfits; 3 hair shades +
  skin tones via RUNTIME recolor (NOT generated). Pipeline probe
  verdict (scratchpad `char-pipeline/`): **method A** — fresh
  `create_character` per combo, standard mode, 4-dir, ~1 gen/combo;
  walk via template `animate_character` ~4 gens/combo. Faces drift
  subtly between combos (they read as siblings — acceptable for a
  "pick a look" creator; mitigate with a FIXED face descriptor per
  gender + same eye color). **Generate hair in a KEYED unnatural color
  (vivid purple)** so hair-shade recolor is clean (natural browns bleed
  into outfits — measured). Optional later upgrade: face-locked
  heroine via states (~481 gens) — owner said "expand when we want".
  The rig stays as the zero-PNG fallback (CLAUDE.md rule 1 dual-path).
- **D-2 Ground = PixelLab `tiles_pro` SEGMENTATION tiles** (probe 2 =
  GO; scratchpad `ground-probe2/final_composite.png`): 32px
  square_topdown, `outline_mode:"segmentation"` (outline mode = grid
  lattice = rejected). Terrain-to-terrain EDGES ARE CODE (Bayer-dither
  alpha mask, 0 gens, `final_composite.js` shows how). Wang
  `topdown_tileset` is REJECTED for grass (always lime; can't chain
  from tiles_pro). Production tuning: ~75% plain tiles, sparse feature
  tiles, tighter tonal range on base grass, dimmer daisies.
- **D-3 Everything-pixels:** after ground lands, audit remaining smooth
  visuals (water shimmer, weather fx, particles, vista/parallax) and
  restyle to chunky pixel language (code, no gens).
- Owner-approved earlier tonight: fable-mode skill (committed
  `79506c7`), daily gen cap, local-only commits.

## Recon results (3 of 4 legs landed; 4th in flight)
- **Docs gap matrix (landed, full detail in its section below):** game
  is genuinely whole (engines/screens/AI/MVP all built). Gaps: quest
  system+log (the one LARGE v1 gap; only guidance engine + AI stub
  exist), skill neglect-decay unbuilt, variety counts below bar (fish
  12, forage 11, recipes 7, flowers 0, trees 3 species, crops 18),
  doc drift (docs still say trees/crops are code-only), 4 open owner
  decisions, wardrobe v1-vs-v2 ambiguity.
- **Char pipeline probe (landed):** see D-1.
- **Ground probes 1+2 (landed):** see D-2. Reusable already-generated
  sets in `ground-probe2/`: sage grass 16 tiles + tilled soil 16 tiles
  (wet/dry/sprout variants) — DO NOT regenerate these two.
- **Puppeteer play-through (IN FLIGHT at handoff time):** driving the
  real game through every screen; will report screen-by-screen
  verdicts, console errors, char/stall/building evidence. Its repo
  leftovers (`package.json`, `package-lock.json`, `recon-driver.mjs`,
  `recon-faces.mjs`) are ITS to clean; if the session died before its
  report, treat as lost — re-run a light QA pass later if needed, and
  don't commit its leftovers; `git checkout -- package.json
  package-lock.json` + delete the driver scripts is safe ONLY after
  confirming no agent is still running.

## THE PLAN — waves

### Wave G (generation, parallel, scratchpad-only — LAUNCHED at handoff)
- **G1 Ground production sets** (~300 cap): re-roll tighter base grass
  + dim daisies; water+shore set; stone-plaza/cobble set; optional path
  set (or reuse soil's plain-dirt); verify with weighted-field mocks.
  Output: `scratchpad/ground-prod/`.
- **G2 Character matrix phase 1** (~450 cap): 2 genders × MEDIUM size ×
  5 hair × 5 outfits = 50 combos, method A, 48px, 4-dir rotations +
  template walk each; KEYED purple hair; fixed per-gender face
  descriptor; drift-review before walk spend. Output:
  `scratchpad/char-matrix/` (+ character IDs ledger file).
- **G3 UI kit** (~350 cap): 6 `create_ui_asset` panels (window frame,
  button set, tabs, dialogue panel, tooltip, dock) in warm wood+cream
  "cozy UO" style; 1 `create_font` (warm pixel font + TTF); 10 NPC bust
  portraits via `create_portrait_character` from the existing
  `characters/<npc>.sheet.png` south rotation crops. Output:
  `scratchpad/ui-kit/`.

### Repo queue R1..R9 (STRICTLY SEQUENTIAL, start after the relevant
gen wave lands; each = own agent, own commit(s), own WORKLOG entry)
- **R1 Character sprite creator** (after G2): copy matrix into
  `src/assets/pixellab/characters/matrix/` (pack with
  `scripts/packsheets.mjs` if loose frames bloat — `assetsInlineLimit`
  is already 0); new selection logic keyed by
  (gender,size,hair,outfit); runtime recolor of keyed-purple hair → 3
  shades + skin tones (reuse `recolorSheet`); rewire
  `ui/charcreation.ts` to the matrix options (5 hair, 3 shades, 5
  outfits, gender; sizes S/L greyed "coming soon" until G-wave 2);
  player + preview render sprite-primary, rig = fallback
  (`CHARACTER_SPRITES_PRIMARY` flips meaning or a new flag — keep ONE
  clear flag); NPCs return to their session-2 sprite sheets
  (`setNpcSpriteMode` back on). Fixes owner complaints #nose/#same/#hair-layer.
- **R2 Ground tiles** (after G1): tile renderer for the pre-rendered
  ground canvas (`world/ground.ts`) using weighted variant scatter
  (seeded), code dither edges between terrains (grass↔path↔tilled↔
  water↔plaza), keep the old painterly painter as the zero-PNG
  fallback. Farm plot + paths + market plaza + shorelines all tiled.
- **R3 Variety data push** (no gens): fish 12→**50** (owner's number;
  season/weather/location/skill framework exists), forage 11→20+,
  recipes 7→20+, crops 18→20; **flowers 0→20**: new ornamental flower
  species table wired into `systems/gardening.ts` (plant/water/bloom/
  sell), reusing the foliage flower sprites for in-world display +
  code icons. Balance prices per `data/` conventions.
- **R4 Stall + building anti-repetition:** move the farm-side stall to
  the market (zones.ts; keep the player's OWN sell-stall mechanic on
  the farm if that's what it is — check `sellCategories`/`shop`
  wiring first; if it's her own stall, keep function, restyle/relocate
  per owner note "אמור להיות דוכן בעיירה"); wire cottage variants 6+8
  + `buildings/spare/*` so no two neighbor buildings repeat; add
  per-building hue/flip jitter where tasteful.
- **R5 Barn use:** barn interaction = storage chest UI (existing
  window system; new `systems/storage.ts`, versioned save key) +
  animals shelter there at night (schedule tweak). Small, real.
- **R6 Quest system + log** (the big one): authored quest data
  (tutorial arc + 3-5 farm/NPC quests), `systems/quests.ts`
  (accept/track/complete, save-versioned), quest-log window, promote
  AI quest stub D3 → real offers behind the AI toggle with scripted
  fallback.
- **R7 Skill neglect-decay** per DECISIONS (slow decay of unused
  skills, floor at tier boundaries, `systems/skills.ts`).
- **R8 Screens re-skin** (after G3): apply UI kit nine-slice panels +
  pixel font + NPC portraits in dialogue window; keep DOM structure,
  swap the chrome. Audit all screens for the "small details" pass.
- **R9 Docs sync:** DECISIONS/HANDOFF/CLAUDE/VISION updated for D-1..3
  (char medium re-reversal!), GAME_OVERVIEW/ROADMAP_TO_V5 status
  flips (trees/crops sprites shipped, this run's additions), WORKLOG
  already per-commit. Log open owner questions (below).
- **Wave G2 (later day, if budget day allows):** body sizes S+L for the
  char matrix (+100 combos ≈ +500 gens incl. walks), NPC roster
  expansion toward the owner's bigger-roster ask (method A + AI
  dialogue seeds), fish/item pixel icons if wanted.

### Order sensitivity
R1 and R2 are independent of R3-R5; R6-R8 can interleave; NOTHING in
the repo runs in parallel with anything else in the repo. Commit after
each R-item (or sub-item if large). If a gen wave hasn't landed, skip
to the next repo item that's unblocked (R3-R7 need no gens at all).

## Open questions FOR THE OWNER (do not decide these)
1. Wardrobe buy+swap in v1 or v2? (DECISIONS reads v1, ROADMAP_TO_V5
   says v2.)
2. Face-locked heroine upgrade (~481 gens) — want it?
3. The 4 open DECISIONS items: festival theme, life-goal list confirm,
   mine location, weather-event specifics (PROPOSALS #16-19 ready).
4. Fish at 50 = data+code icons now; generate 50 pixel fish ICONS too
   (~50-100 gens)? Recommended but her call on style.

## Exact resume state (updated by the R1 repo agent)
- **R1 is DONE** (local commit `0c7aea2`, never pushed, WORKLOG entry
  "character — curated sprite matrix becomes the shipped player look"): the
  50-combo matrix is packed into `src/assets/pixellab/characters/matrix/`
  (`scripts/packsheets.mjs --matrix`), `CHARACTER_SPRITES_PRIMARY=true` now
  means the matrix player + NPC-sprite mode, `spriteChar.ts` rewritten as the
  matrix bridge (keyed-purple hair → 3 naturals, rig = zero-PNG fallback),
  `charcreation.ts` shows the sprite creator (5 hair / 5 outfit / 3 shade;
  skin-tone + body-size "coming soon"), `meta.ts` schema+revive extended (old
  saves → default matrix look). No combos excluded (ponytail-tunic re-check
  passed). Verified via headless Edge (screenshots in
  `scratchpad/char-matrix/ingame-*.png` + `crop-*.png`). Skin recolour shipped
  OFF (mechanism preserves lightness, can't darken skin — honest call).
  Follow-ups logged in the WORKLOG (unused legacy heroine sheets; S/L sizes +
  lightness-aware skin as the next matrix expansion).
- **R3, R4, R5, R7 are DONE — local commits only (never pushed), each its own
  commit + WORKLOG entry:**
  - `619144b` R3 Fish 12→50
  - `61d3fee` R3 Forage 11→26
  - `4facc09` R3 Recipes 6→21
  - `b731ddb` R3 Crops 18→20 (dual-path proven)
  - `c479f92` R3 Flowers 0→20 (new `data/flowers.ts` + species gardening system)
  - `49593b4` R4 Stall → market edge + cottage anti-repetition (variants 6/8, flip)
  - `372cad3` R5 Barn storage chest + animals shelter at barn at night
  - `2702f98` R7 Skill neglect-decay, floored at tier boundaries
  - `6adeacc` R2 Ground → pixel tiles (grass/soil/water/plaza), dual-path + code
    Bayer-dither edges; painterly painter kept as the zero-PNG fallback; the
    64 G1 tiles copied into `src/assets/pixellab/ground/`. Verified tiled +
    fallback via headless Edge (`scratchpad/ground-prod/ingame-*.png`,
    `verify_fallback.png`). See WORKLOG "world — the ground becomes pixel tiles".
- Every item verified per fable-mode (esbuild/node assertions for data + logic;
  headless-Edge screenshots for the crops fallback, flower beds+icons, the
  market/stall, and the barn storage window). `npm run build` green at each.
- **Working tree still holds the puppeteer agent's temp files** (`package.json`,
  `package-lock.json`, `recon-driver.mjs`, `recon-faces.mjs`) — NEVER committed
  by this agent (selective `git add` of own files only). Still not ours to
  commit or revert.
- **R6 is DONE — the quest system + log — 5 local commits (never pushed), each
  its own WORKLOG entry:**
  - `4343dfb` R6/1 core: `data/quests.ts` (6 authored quests + 3 AI templates,
    step kinds collect/deliver/talk/catch/harvest/reach via activity-counter +
    possession-gather flavours) + `systems/quests.ts` engine (accept/track/
    progress/turn-in/abandon/auto-complete, effect-return pattern) + `QUESTS_KEY`
    (in saves.ts GAME_KEYS).
  - `cf3dfdd` R6/2 wired into `main.ts` — event seams beside the existing
    `fireGuidance` calls (catch/forage/harvest/busk/cook/sell/talk/reach),
    reward application (coins/items/Friendship+heart), possession live-check,
    New Game wipe, save, dev bridge.
  - `fac1fd6` R6/3 quest-log WINDOW (`ui/questlog.ts`, icon 📋 / key J) —
    Active/Completed tabs, step checklist + counts, reward preview, abandon;
    a "Getting Started" box MIRRORS the live Guidance layer (coexist, not
    duplicated); subtle step-complete toast.
  - `f34bd29` R6/4 dialogue integration — offers/turn-ins as the giver's opening
    choices (`questOptions` hook in `dialoguebox.ts`, `systems/dialogue.ts` stays
    pure); accept/turn-in updates Friendship per rewards.
  - `8d44aaf` R6/5 D3 AI dynamic offers (`ai/features/questOffers.ts`, replaces
    `questStub.ts`) — validated against the closed schema (questId ∈ AI
    templates), reward clamped, scripted fallback on any failure, byte-identical
    with AI off. Verified: node harnesses (engine 33 asserts, offers 13 asserts)
    + live headless-Edge/puppeteer (accept→gather→ready→turn-in→rewards→reload→
    New-Game-wipe; dialogue offer/accept/decline; AI-off provider `none` + 0
    anthropic.com calls; AI-on offer surfacing). Screenshots in
    `scratchpad/quests/`.
- **R8 is DONE — screens re-skin + the whole polish backlog — 7 local commits
  (never pushed), each its own WORKLOG entry:**
  - `7385a64` R8 Mission A — screens re-skin with the G3 UI kit: new
    `src/ui/skin.ts` (dual-path panel/font loader), wood-frame `border-image`
    on every utility/panel window + the menu/pause/char-creation modals (dark
    readable interiors, no fill), parchment tooltip on `#prompt`,
    `WildhearthStorybook` font on titles/buttons via `--font-title`, and
    Maren/Tobin portrait busts in the dialogue notch (data-driven; other 8 NPCs
    gracefully portrait-less). Kit assets under `src/assets/pixellab/ui/` +
    `src/assets/fonts/`. Verified incl. a zero-UI-PNG fallback boot.
  - `f23c9b8` R8/B1 — Skills window shows `skillTier` (Novice/Skilled/Expert).
  - `8f3418d` R8/B3 — `STORAGE_KEY` folded into `saves.ts` GAME_KEYS.
  - `d3fb35f` R8/B2 — deleted the 5 unused legacy `characters/heroine*` sheets
    (~968KB; matrix replaced them, rig fallback intact).
  - `6ee70b5` R8/B4+B5 — plain-furrow-dominant field (`SOIL_TILLED_BAG`),
    flower/grass scatter off the plaza cobble (ground.ts + scatter.ts), and
    chunky pixel water glints (`props.ts drawWaterShimmer/drawOpenWaterShimmer`).
  - `ba7f904` R8/B6 — active-quest count badge on the quest dock icon.
  - NOT shipped from the kit (honest calls, in the Mission-A WORKLOG Follow-ups):
    the `button`/`tabs` panels (gold CSS + storybook font already read as one
    family; the wood plaque looked gaudier) and the `dialogue-panel` PNG (the
    generic window frame + a CSS notch is simpler than nine-slicing a baked
    notch). 8 more NPC portraits + S/L body sizes remain for the next budget day.
- **Repo queue remaining:**
  R9 (docs sync incl. stale `char-medium-code-rig.md` AND the char-medium
  re-reversal to matrix sprites; GAME_OVERVIEW/ROADMAP_TO_V5 quest-system status
  flip — v1's one LARGE gap is now CLOSED). **R1–R8 are DONE.**
- PixelLab: untouched by this agent (0 gens). Balance unchanged from the G-wave
  agents' accounting.

## Notes for the next repo agent
- **R8 tie-in:** R7 exposed `skillTier(value)` (Novice/Skilled/Expert) but it
  isn't shown in the Skills window yet — easy win for the screens pass.
- **R6/quests shipped:** the quest log is a real window-system citizen; the
  `talk` and `reach` step kinds are supported + unit-tested but no SHIPPED quest
  uses them yet (easy content adds). The AI offer's clamped reward is validated
  + stored but the granted payout stays the authored template's (balance-safe;
  a future refinement could let the accepted instance carry the clamped reward).
  `STORAGE_KEY` (R5) is still missing from `saves.ts` GAME_KEYS — harmless
  (New Game re-seeds via `resetStorage`) but worth folding in during R9.
- **Data tables are fully table-driven** — fish/forage/recipes/crops/flowers all
  propagate to prices/names/icons/collections/shop with zero per-item code, so
  future variety is just more rows.
- No open owner-decisions were hit; nothing was skipped for lack of a decision.

## Next step (the very first thing the continuation does)
R1–R8 are DONE (all local commits, never pushed). The ONLY remaining repo
item is **R9** (docs sync): update DECISIONS/CLAUDE/VISION for the
char-medium re-reversal (the stale `char-medium-code-rig.md`), and flip the
quest-system status in GAME_OVERVIEW/ROADMAP_TO_V5 now that v1's one LARGE
gap is closed. `STORAGE_KEY` was already folded into saves.ts GAME_KEYS
(R8/B3). Also worth a doc note: the UI kit is now wired (src/ui/skin.ts,
dual-path) and 8 more NPC portraits + S/L body sizes are the next
budget-day generation items (recipe in the kit LEDGER). Verify per
fable-mode, commit locally per item, update this file's resume state.

---

## RUN COMPLETE (2026-07-11, R9 docs-sync agent)

**R9 is DONE — every repo queue item (R1 through R9) has now shipped, each
its own local commit (never pushed), each with a WORKLOG entry.** This run
is closed out. (No `char-medium-code-rig.md` file was ever found in the
repo — the "stale doc" note above turned out to refer to
`docs/DECISIONS.md`'s "Art medium division" section itself, which is what
got annotated + superseded, not a separate file.)

### What R9 touched (docs only, one commit)
- **`docs/DECISIONS.md`** — annotated the session-3 "Art medium division"
  entry as superseded (kept in full, not deleted); added three new
  sections: "Art medium division — FINAL FLIP" (D-1, characters = curated
  sprite matrix), "Ground medium" (D-2, `tiles_pro` segmentation, Wang
  rejected), and "Everything-one-pixel-medium rule" (D-3).
- **`CLAUDE.md`** hard rule #1 — MEDIUM DIVISION clause rewritten: characters
  = sprite matrix (rig = fallback), environment = sprites (unchanged), ground
  = PixelLab tiles (painterly = fallback).
- **`docs/HANDOFF.md`** — new top "SESSION 4" section: the re-flip + probe
  one-liners, the full commit list, PixelLab spend, next-day queue.
- **`docs/GAME_OVERVIEW.md`** — Quests flipped 🟡→🟢 (full rewrite of that
  section), skill neglect-decay flipped to built, Collections variety counts
  updated (fish 50, forage 26, +flowers 20, recipes 21, crops 20 mentioned in
  context), barn storage noted under Housing, UI re-skin noted under Menus,
  the Art direction section rewritten for the FINAL medium (characters =
  matrix, ground = tiles), and the Character-creation section rewritten for
  the matrix (replacing the stale honest-coverage-table language).
- **`docs/ROADMAP_TO_V5.md`** — flagged the "Current build state (mid-v1)"
  section as a stale session-1 snapshot (pointing to GAME_OVERVIEW for
  current truth) while still refreshing its variety numbers; quests bullet
  marked BUILT; skill-decay bullet marked BUILT; the v1 "Art / visual layer"
  bullet and the "Segmented rig scope creep" risk note both rewritten for
  the session-4 FINAL flip; v4/v5 Art bullets updated from "characters = rig"
  to "characters = sprite matrix".
- **`docs/VISION.md`** — the "Medium division" paragraph under Art direction
  rewritten for the FINAL flip (light touch, one paragraph, per the brief).
- **`docs/PIXELLAB_ASSETS.md`** — §1 table's heroine/NPC rows updated (matrix
  + ground tiles + UI kit added, old heroine row removed since those files
  are deleted); a new "Wave 7" entry in §2 documenting the char-matrix/
  ground-tile/UI-kit generation recipes (keyed-purple hair convention, the
  4-dir cardinal-only matrix rotations, the Wang-tileset rejection, the UI
  panels shipped-vs-not); §3's folder tree updated with `matrix/`, `ground/`,
  `ui/`, and the fonts note; a new "Recolouring the matrix hair" section
  (the old "Recolouring the heroine" section kept, marked SUPERSEDED, for
  its underlying technique); §4's "add a hairstyle" recipe split into the
  superseded heroine path and the live matrix path; §5's cost ledger got a
  new "Wave 7" paragraph (~520 gens, Tier 3, balance ~8,063).

### Contradictions found and how they were reconciled
1. **Crops/trees were already sprite-sourced** (`src/assets/pixellab/crops/`
   and `trees/` both have real ripe/season PNGs) even though
   `GAME_OVERVIEW.md`'s Art-direction section still said "still code-only /
   not built... an approved MIX path... gated behind its own batch, not yet
   run." This predates session 4 (nobody in this run generated crop/tree
   sprites) — some earlier session shipped them without syncing this
   paragraph. Fixed while rewriting that section anyway (in-scope: the
   mission named "ground+characters art rows reflect the final mediums" and
   this row sits right next to them).
2. **`ROADMAP_TO_V5.md`'s "Current build state (mid-v1)" section** claims
   the NPC engine, Needs, Relationships, Dialogue+AI, Character Creation,
   Guidance Mode, the Festival, and the visual foundation are all "Not yet
   built" — all of these have been built and shipped for many sessions.
   This is a session-1 snapshot that was never revisited as the game grew;
   fully rewriting it was judged out of scope for a targeted docs-sync run
   (would be a large, unrelated rewrite), so it's flagged in place with a
   pointer to `GAME_OVERVIEW.md` (which IS kept current) rather than left
   silently wrong or silently deleted.
3. **The "stale `char-medium-code-rig.md`"** named in the R9 instructions
   does not exist as a file — grepped the whole repo, zero matches. Read as
   referring to `docs/DECISIONS.md`'s "Art medium division" section, which
   is exactly what got annotated + superseded.

### Verification
`npm run build` green after every edit batch (docs-only changes, so this
mainly confirms nothing else in the working tree broke) — final check run
after all seven doc files were edited. No source files touched, per the
R9 standing order ("Docs only — NO source changes").

### Working tree note (unchanged from before R9)
`package.json`/`package-lock.json` (modified) and `recon-driver.mjs`/
`recon-faces.mjs` (untracked) — the puppeteer QA agent's leftovers — are
STILL sitting in the working tree, STILL not staged or touched by this
agent (selective `git add` of only the docs this entry lists). Its recon
never reported back before this run ended; see HANDOFF.md's SESSION 4
next-day queue item 5.

**The overnight run (SESSION 4) is now fully closed out: R1 through R9,
all shipped, all local-only commits, nothing pushed.**

---

## DAY 2 (2026-07-11) — v2 progression begins, one block at a time

Standing orders unchanged: **LOCAL COMMITS ONLY, NEVER PUSH**; `npm run build`
green per commit; WORKLOG entry per commit; selective `git add` (the puppeteer
agent's `package.json`/`package-lock.json`/`recon-driver.mjs`/`recon-faces.mjs`
are STILL in the working tree, STILL never ours to stage). 0 PixelLab gens.

### v2 BLOCK #1 — DONE: "Customers come to your stall"  (commit `bc6767d`)
- **Which block the roadmap defined as #1, and why:** the supervisor guessed
  "the real sell menu," but that is **already built** (v1: `ui/shopwindow.ts`
  is a full stepper/total/sell-all/category-aware window with an NPC-stall
  mode). Per `ROADMAP_TO_V5.md` §v2's economy delta + the global dependency
  spine (`Schedules → Customers → Reputation → Town`), the genuinely-first
  UNBUILT v2 block is **`systems/customers.ts` — NPCs walk to your stall with
  want-table-driven demand** (customers.ts + reputation.ts were both absent).
  It is ALSO the "deepened own-stall selling" the brief described.
- **What shipped:** townsfolk in the plaza walk to her own stall during market
  hours, wanting a good she holds (role-based want-tables), paying a 1.3×
  premium; a "Customers at your stall" section in the trade window; serving =
  coins + the existing sell seam (guidance/quests/day-log/arcs, extracted to a
  shared `logSale()`) + Friendship +1 + memory; 🛒 bubbles; a daily served cap;
  patience timeout (no penalty — that's reputation's job). New
  `systems/customers.ts`, `Npc.visit` override, `economy.sellGoodAt`,
  `CUSTOMERS_KEY` in GAME_KEYS, `CUSTOMER_*` knobs. Old saves safe. Full detail
  in the WORKLOG entry of the same title.
- **Bonus bug fixed (in-scope, surfaced by this block):** the scale-window
  measures content height once at init while the shop's sell/buy lists are
  EMPTY, so on a fresh profile the trade window opened collapsed (~27px body,
  clipping everything — the base sell/buy too). Fixed inside the shop window:
  bounded scroll regions + `fitHeight()` on open. Confirmed the window now
  opens correctly filled (screenshots in scratchpad `v2-block1/`).
- **Verdicts (honest):** logic + serve + persistence + organic AND forced spawn
  all verified live (headless Edge + `__wh`); build green. The premium is a
  flat 1.3× (reputation will modulate it in block #2). Customer visits are
  runtime-only (a mid-market reload clears the queue — harmless).

### v2 BLOCK #2 (next, for the supervisor to dispatch) — Reputation / Fame
Per the spine (`Customers → Reputation → Town`) and `ROADMAP_TO_V5.md` §v2
("`systems/reputation.ts` — town-wide Fame, independent of any one NPC and of
Haggling"), the next block is **town-wide Reputation/Fame** (VISION §Economy:
UO fame/karma — a single town-facing number shifting general treatment). It
should hook into THIS block's seams: reputation rises when customers are served
(and could fall when they leave unserved — the penalty deliberately deferred
here), and modulates the customer premium / spawn rate / who shows up. Then
**Town buildout** (regions + merchants) is the larger block after that.
